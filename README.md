# 《山海夜市》(Shanhai Night Market) - Web Survivor Game

> 一款基于 Phaser 4.2.1 + TypeScript + Vite 构建的 2D 俯视角战斗厨房类幸存者游戏。

---

## 1. 游戏特色与卖点

- **题材与玩法**：玩家扮演一名夜市摊主，在妖怪围攻的山海夜市中穿梭移动、自动攻击、收集“热度”（经验）和“食材”（金币），组合厨具和口味，构筑出夸张的“战斗厨房”，挑战 12 波幽冥百鬼与最终饕餮 Boss。
- **独创构筑体系**：
  - **厨具**（攻击几何与模式）：铁锅（近战挥砍）、菜刀（飞刀回旋）、竹签（穿透疾刺）、炉火（地面燃烧）、调料瓶（环绕护体）、上菜铃（召唤帮厨小幽灵）。
  - **口味**（属性与词条）：红辣椒（火/暴击）、老冰块（冰/减速）、芝麻油（移速/暴击）、古法蔗糖（掉落加成）、特酿豆瓣酱（伤害倍率）、蒸笼（护甲生命）等。
  - **神厨菜谱**（标签质变）：达成特定厨具与口味标签后触发质变转化（如：爆炒火环、冰镇串烧、拔丝烈焰、热油飞刀等）。
- **极速循环**：单局 10~15 分钟，波次间 15~20 秒夜市整备开店，死亡或胜利后 10 秒内即可快速重开。

---

## 2. 技术架构与高性能特性

- **游戏引擎**：Phaser 4.2.1（精确版本锁定）
- **物理与计算优化**：
  - 固定 60Hz 步长模拟时钟（`SimulationClock`），单帧最多 4 步补算，切后台清空累积防跳跃。
  - 带种子的 PRNG（`SeededRNG`，Mulberry32 算法），保证相同 Seed 100% 可重现。
  - 2D 空间哈希网格（`SpatialHash`），将 O(N^2) 碰撞检测降为 O(1) 局部近邻查询，实测支持 500+ 敌人与 1000+ 投射物稳定 60 FPS。
  - 核心对象全量池化（`ObjectPool`：敌人、投射物、经验/食材掉落物、伤害飘字）。
- **表现层与音频**：
  - 强类型领域事件总线（`EventBus`），彻底解耦物理模拟与表现渲染。
  - Web Audio 动态合成音效与用户首次交互手势解锁机制。
  - 移动端跟手虚拟摇杆与响应式 960x540 FIT 视口缩放。
- **数据与存档**：
  - 全量内容 Zod Schema 校验与跨表引用完整性检查。
  - localStorage 安全持久化存储，支持结构校验、安全降级与版本迁移。

---

## 3. 开发与构建指令

```bash
# 1. 安装依赖 (推荐 pnpm)
pnpm install

# 2. 启动本地开发服务器 (默认端口 3000)
pnpm dev

# 3. 运行 TypeScript 严格类型检查
pnpm typecheck

# 4. 运行 ESLint 代码检查
pnpm lint

# 5. 运行 Vitest 单元测试
pnpm test

# 6. 运行 Playwright 端到端冒烟测试
pnpm test:e2e

# 7. 打包生产静态资源 (输出至 dist/)
pnpm build
```

---

## 4. 游戏操作指南

- **键盘操作**：
  - 移动：`W` `A` `S` `D` 或 `↑` `←` `↓` `→`
  - 暂停：`Esc`
  - 开发者调试面板：`~`（波浪键）
- **触屏操作**：
  - 左半屏幕滑动唤起跟手虚拟摇杆控制移动。
- **作弊与调试指令**：
  - 按 `~` 打开调试面板，可随时切换无敌、获得食材、跳至下波、一键刷怪、2x加速等。
